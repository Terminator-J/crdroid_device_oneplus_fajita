<?xml version="1.0" encoding="utf-8"?>
<!--
     SPDX-FileCopyrightText: 2006 The Android Open Source Project
     SPDX-FileCopyrightText: 2024-2026 crDroid Android Project
     SPDX-License-Identifier: Apache-2.0
-->
<resources>
    <!-- Default device corner radius, used for assist UI -->
    <dimen name="config_rounded_mask_size">110px</dimen>

    <!-- Microns/ums (1000 um = 1mm) per pixel for the given device. If unspecified, UI that
        relies on this value will not be sized correctly. -->
    <item name="pixel_pitch" format="float" type="dimen">63.17</item>

    <!-- Udfps fingerprint animation -->
    <dimen name="udfps_animation_offset">0dip</dimen>

    <!-- the padding on the start of the statusbar -->
    <dimen name="status_bar_padding_start">0.0px</dimen>

    <!-- the padding on the end of the statusbar -->
    <dimen name="status_bar_padding_end">5.0px</dimen>

    <!-- Margin on the left side of the carrier text on Keyguard -->
    <dimen name="keyguard_carrier_text_margin">72.0px</dimen>

    <!--  Total minimum padding to enforce to ensure that the dot can always show  -->
    <dimen name="ongoing_appops_dot_min_padding">22.6dp</dimen>

    <!-- Insert description here (guess we need something like this) -->
    <dimen name="rounded_corner_content_padding">26.1dp</dimen>
</resources>
